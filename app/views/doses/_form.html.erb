<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient_id, collection: Ingredient.all.order(:name) %>
  <%= f.submit value: "Add", class: "btn btn-danger" %>
<% end %>
